<div class="row">
  <div class="col-2">
    <h4> Name </h4>
  </div>
  <div class="col-4">
    <h4> Description </h4>
  </div>
  <div class="col-2">
    <h4> Country </h4>
  </div>
  <div class="col-2">
    <h4> Language </h4>
  </div>
</div>

  <% @sources.each do |source| %>
  <div class="row">
    <div class="col-2">
      <%= source.name %>
    </div>
    <div class="col-4">
      <%= source.description %>
    </div>
    <div class="col-2">
      <%= source.country %>
    </div>
    <div class="col-2">
      <%= source.language %>
    </div>
     <div class="col-2">
    <%= link_to 'Edit', edit_source_path(source),  class: "btn btn-outline-primary me-2" %>
    </div>
  </div>
  <% end %>
</p>
  <%= link_to 'Add sources', sources_path(:add_marker => 'yes'), class: "btn btn-outline-primary me-2" %>
  <% if params[:add_marker] == 'yes'%>
    <%= render 'form', source: @source %>
  <% end %>

